<p>
  <%= link_to l(:label_settings), { controller: 'settings', action: 'plugin', id: 'redmineup_tags' } %>
  — plugin-wide tag display options (sidebar style, colors, sort).
</p>
<p>
  Tag clouds are managed per project:
  <strong>Project → Settings → <%= l(:tag_clouds) %></strong>.
</p>
